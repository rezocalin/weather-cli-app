# Simple Weather CLI App

A lightweight Python Command Line Interface (CLI) application that fetches real-time weather information for any city using the `wttr.in` API.

## Features
- Fetches current weather data instantly.
- Simple and clean terminal output.
- No complex API keys required.

## How to Run
1. Make sure you have Python installed.
2. Install the required dependencies:
   ```bash
   pip install requests
